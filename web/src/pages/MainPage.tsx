import React, { FC } from "react";
import { Layout } from "../components/Layout";

export const MainPage: FC = () => {
    return (
        <Layout>
            <h1>JISPEN MAIN PAGE</h1>
        </Layout>
    );
};
